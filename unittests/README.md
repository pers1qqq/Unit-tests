# Отчёт по лабораторным работам №4–5  
## Тема: Unit-тестирование и CI/CD (GitHub Actions)

---

# 📌 Лабораторная работа №4  
## **Написание Unit-тестов**

### 🎯 Цели и задачи тестирования
Основная цель — проверить корректность работы функций вычисления геометрических параметров фигур, реализованных в модуле `geometric_lib`.

Задачи:
- Проверить корректность вычислений площади и периметра фигур.
- Выявить ошибки в исходном коде.
- Освоить инструмент модульного тестирования `unittest`.
- Получить навык автоматизации тестирования.

---

# 🧩 Описание тестируемого продукта

Тестируемый проект состоит из библиотеки `geometric_lib`, содержащей функции:

### **Прямоугольник (`rectangle.py`)**
```python
def area(a, b):
    return a * b

def perimeter(a, b):
    return 2 * (a + b)
```

### **Круг (`circle.py`)**
```python
import math

def perimeter(r):
    return 2 * math.pi * r

def area(r):
    return math.pi * r * r
```

### **Квадрат (`square.py`)**
```python
def area(a):
    return a * a

def perimeter(a):
    return 4 * a
```

### **Треугольник (`triangle.py`)**
```python
def area(a, h):
    return 0.5 * a * h

def perimeter(a, b, c):
    return a + b + c
```

Библиотека должна корректно работать с нулевыми, целыми и вещественными значениями.

---

# 🧪 Область тестирования

Unit-тестирование включает проверку функций:

- `rectangle.area`, `rectangle.perimeter`
- `circle.area`, `circle.perimeter`
- `square.area`, `square.perimeter`
- `triangle.area`, `triangle.perimeter`

НЕ тестируется:
- ввод/вывод
- взаимодействие с пользователем
- исключения
- файловая система

---

# 🧠 Стратегия тестирования

Используется модуль `unittest`.

Особенности стратегии:
- Тестирование проводится изолированно для каждой функции.
- Используются проверки:
  - `assertEqual`
  - `assertAlmostEqual`
- Тесты организованы в отдельной директории `unittests/`.
- Каждый файл тестов — отдельный набор тестов для фигуры.

---

# 📝 Пример тестового файла (test_circle.py)

```python
import os
import sys
import unittest
import math

CURRENT_DIR = os.path.dirname(__file__)
PROJECT_ROOT = os.path.abspath(os.path.join(CURRENT_DIR, ".."))
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)

import geometric_lib.circle as circle

class CircleTestCase(unittest.TestCase):
    def test_perimeter_zero_radius(self):
        res = circle.perimeter(0)
        self.assertEqual(res, 0)

    def test_perimeter_unit_radius(self):
        res = circle.perimeter(1)
        self.assertAlmostEqual(res, 2 * math.pi, places=7)

    def test_area_unit_radius(self):
        res = circle.area(1)
        self.assertAlmostEqual(res, math.pi, places=7)

if __name__ == "__main__":
    unittest.main()
```
---

# Разбор файла `test_circle.py`

Короткое, чёткое объяснение того, что делает каждая строка.

---

```python
import os
import sys
import unittest
import math
```
Импорт модулей: работа с путями, системный путь, unittest и математика.

---

```python
CURRENT_DIR = os.path.dirname(__file__)
```
Путь к папке, где находится текущий тестовый файл.

---

```python
PROJECT_ROOT = os.path.abspath(os.path.join(CURRENT_DIR, ".."))
```
Путь к корню проекта (на один уровень выше папки unittests).

---

```python
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)
```
Добавляем корень проекта в пути поиска модулей, чтобы работал импорт `geometric_lib`.

---

```python
import geometric_lib.circle as circle
```
Импортируем модуль circle.py.

---

```python
class CircleTestCase(unittest.TestCase):
```
Создаём класс тестов (обязательное наследование от unittest.TestCase).

---

```python
    def test_perimeter_zero_radius(self):
```
Тест 1: проверка периметра круга с радиусом 0.

```python
        res = circle.perimeter(0)
        self.assertEqual(res, 0)
```
Ожидаем, что периметр равен 0.

---

```python
    def test_perimeter_unit_radius(self):
```
Тест 2: периметр при радиусе 1.

```python
        res = circle.perimeter(1)
        self.assertAlmostEqual(res, 2 * math.pi, places=7)
```
Проверяем, что значение совпадает с 2π с точностью до 7 знаков.

---

```python
    def test_area_unit_radius(self):
```
Тест 3: площадь круга при радиусе 1.

```python
        res = circle.area(1)
        self.assertAlmostEqual(res, math.pi, places=7)
```
Проверяем, что площадь равна π.

---

```python
if __name__ == "__main__":
    unittest.main()
```
Запуск тестов, если файл запускается напрямую.


---

# 🏁 Критерии приёмки тестов

- Все тесты проходят (`OK`).
- Тесты охватывают все функции библиотеки.
- Структура каталога соответствует требованиям.
- Тесты могут выполняться автоматически через CI.

---

# 📘 Результаты тестирования

Ожидаемые результаты:
- вывод о корректности работы функций;
- выявление ошибок (если есть);
- зелёный отчёт об успешном выполнении тестов.

---

---

# 🚀 Лабораторная работа №5  
## **Настройка CI/CD с помощью GitHub Actions**

---

# 🎯 Цель работы

Настроить автоматический запуск unit-тестов при каждом обновлении репозитория с помощью GitHub Actions.

---

# 🔧 Реализация CI/CD

Создан файл workflow:

```
.github/workflows/main.yml
```

Его задача — автоматически запускать тесты на двух ОС: Ubuntu и Windows.

---

# 📄 Полный текст файла `main.yml`

```yaml
name: Run Unit Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  test:
    name: Run tests on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run unit tests
        shell: bash
        run: |
          PYTHONPATH=. python -m unittest discover -s unittests -p "test_*.py"
```

---

# 📌 Подробный разбор файла main.yml

### **1. name**
Название workflow, отображается в GitHub Actions.

### **2. on: push / pull_request**
Запуск CI при обновлении веток и PR.

### **3. matrix**
Выполнение тестов на Ubuntu и Windows.

### **4. Checkout**
Клонирование репозитория.

### **5. setup-python**
Установка Python 3.11.

### **6. Запуск тестов**
Команда запускает поиск и выполнение всех тестов.

---

# 🧾 Выводы

В ходе выполнения двух лабораторных работ были:
- разработаны unit-тесты для библиотеки геометрических функций;
- выявлены и исправлены ошибки;
- настроен CI/CD-процесс на GitHub Actions;
- обеспечена автоматическая проверка проекта на двух ОС;
- сформирована надёжная инфраструктура тестирования.
